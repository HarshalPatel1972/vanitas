'use client';

export default function Experience() {
  return (
    <div className="flex items-center justify-center w-full h-screen bg-black text-white">
      <h1 className="text-2xl font-mono">Ready for new prompt.</h1>
    </div>
  );
}
